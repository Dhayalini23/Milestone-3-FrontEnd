import { FilterSubscriptionPipe } from './filter-subscription.pipe';

describe('FilterSubscriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSubscriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
